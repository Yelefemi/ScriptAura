# Copy this file to `.env` and fill in values. Never commit your real .env to source control.
# Example using Gmail SMTP (for Gmail consider an app password and allowless secure apps options may be needed)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password-or-app-password

# Destination email where contact messages will be delivered. If empty, SMTP_USER will be used.
# By default the code will deliver to `yelefemi111@gmail.com` if no `TO_EMAIL` is set.
TO_EMAIL=owner@example.com

# Optional: server port
# PORT=3000

# SendGrid (serverless) configuration - replace or set these instead of SMTP if using SendGrid
SENDGRID_API_KEY=your-sendgrid-api-key
# FROM_EMAIL is the verified sender address in SendGrid. If empty, TO_EMAIL will be used as sender.
FROM_EMAIL=no-reply@example.com
